apiVersion: v1
kind: Namespace
metadata:
  name: actions-runner-system
  labels:
    # Kubernetes mode doesn't require privileged containers (more secure)
    # baseline allows the runner pod, restricting only dangerous capabilities
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/warn: baseline
