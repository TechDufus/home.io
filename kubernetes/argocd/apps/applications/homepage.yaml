apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homepage
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Homepage Helm chart
    - chart: homepage
      repoURL: https://jameswynn.github.io/helm-charts
      targetRevision: "1.2.3"  # Latest stable version
      helm:
        releaseName: homepage
        valueFiles:
          - $values/kubernetes/argocd/values/homepage.yaml
    # Values source from Git repository
    - repoURL: https://github.com/TechDufus/home.io
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: homepage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 3